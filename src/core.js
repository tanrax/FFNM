document.addEventListener('DOMContentLoaded', () => {

    //===
    // INIT
    //===

    // Syntax
    validateSyntax();
    // Events
    addEventClick();
    addEventScroll();
    addEventHover();
    addEventVisible();

});
